import fs from'node:fs';import path from'node:path';import sqlite from'node:sqlite';import{SqliteStore}from'./sqlite-store.js';import type{RunnerJobInput,RunnerRegistrationInput,VerificationJobInput,VerificationJobStatus}from'./store.js';import{newId,nowIso}from'../../shared/src/ids.js';import{MadeProofError}from'../../shared/src/errors.js';const{DatabaseSync}=sqlite as any;function parse<T>(v:unknown,f:T):T{if(v==null)return f;if(typeof v==='string')return JSON.parse(v)as T;return v as T}
export class SqliteDistributedStore extends SqliteStore{private readonly q:any;constructor(readonly distributedDataDir:string){super(distributedDataDir);this.q=new DatabaseSync(path.join(distributedDataDir,'madeproof.db'));this.q.exec('PRAGMA foreign_keys = ON; PRAGMA journal_mode = WAL; PRAGMA busy_timeout = 5000;')}override migrate(){super.migrate();const f=path.resolve('packages/db/migrations/sqlite/0002_worker_runner.sql');if(fs.existsSync(f))this.q.exec(fs.readFileSync(f,'utf8'))}ping(){return true}override close(){try{this.q.close()}finally{super.close()}}
registerRunner(i:RunnerRegistrationInput){const id=newId('rnr'),at=nowIso();this.q.prepare('INSERT INTO runner_registrations(id,workspace_id,name,credential_hash,version,capabilities_json,created_at) VALUES(?,?,?,?,?,?,?)').run(id,i.workspaceId,i.name,i.credentialHash,i.version,JSON.stringify(i.capabilities),at);return{id,workspace_id:i.workspaceId,name:i.name,version:i.version,capabilities:i.capabilities,created_at:at,last_heartbeat_at:null,revoked_at:null}}listRunners(w:string){return(this.q.prepare('SELECT id,workspace_id,name,version,capabilities_json,last_heartbeat_at,revoked_at,created_at FROM runner_registrations WHERE workspace_id=? ORDER BY created_at DESC').all(w)as any[]).map(r=>({...r,capabilities:parse<string[]>(r.capabilities_json,[])}))}getRunnerByCredentialHash(h:string){const r=this.q.prepare('SELECT * FROM runner_registrations WHERE credential_hash=? AND revoked_at IS NULL').get(h)as any;return r?{...r,capabilities:parse<string[]>(r.capabilities_json,[])}:null}revokeRunner(w:string,id:string){const n=nowIso(),r=this.q.prepare('UPDATE runner_registrations SET revoked_at=? WHERE id=? AND workspace_id=? AND revoked_at IS NULL').run(n,id,w);this.q.prepare("UPDATE runner_jobs SET status='CANCELLED',updated_at=?,lease_token_hash=NULL,lease_expires_at=NULL WHERE runner_id=? AND workspace_id=? AND status IN ('LEASED','RUNNING')").run(n,id,w);return r.changes===1}heartbeatRunner(w:string,id:string,v:string,c:string[]){const r=this.q.prepare('UPDATE runner_registrations SET last_heartbeat_at=?,version=?,capabilities_json=? WHERE id=? AND workspace_id=? AND revoked_at IS NULL').run(nowIso(),v,JSON.stringify(c),id,w);if(r.changes!==1)throw new MadeProofError('RUNNER_REVOKED','Runner credential is revoked or invalid',401)}
enqueueVerificationJob(i:VerificationJobInput){const e=this.q.prepare('SELECT * FROM verification_jobs WHERE workspace_id=? AND run_id=?').get(i.workspaceId,i.runId)as any;if(e)return e;const id=newId('vjob'),n=nowIso();this.q.prepare('INSERT INTO verification_jobs(id,workspace_id,run_id,actor_id,status,attempt,max_attempts,available_at,request_key,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?)').run(id,i.workspaceId,i.runId,i.actorId,'QUEUED',0,i.maxAttempts??5,n,i.requestKey??null,n,n);return this.q.prepare('SELECT * FROM verification_jobs WHERE id=?').get(id)}getVerificationJob(w:string,r:string){return(this.q.prepare('SELECT * FROM verification_jobs WHERE workspace_id=? AND run_id=?').get(w,r)as any)??null}claimVerificationJob(workerId:string,leaseSeconds:number){const n=nowIso(),x=new Date(Date.now()+leaseSeconds*1000).toISOString();this.q.exec('BEGIN IMMEDIATE');try{const r=this.q.prepare("SELECT * FROM verification_jobs WHERE ((status='QUEUED' AND available_at<=?) OR (status IN ('CLAIMED','RUNNING','WAITING_RUNNER') AND claim_expires_at IS NOT NULL AND claim_expires_at<?)) AND attempt<max_attempts ORDER BY created_at LIMIT 1").get(n,n)as any;if(!r){this.q.exec('COMMIT');return null}this.q.prepare("UPDATE verification_jobs SET status='CLAIMED',claimed_by=?,claim_expires_at=?,last_heartbeat_at=?,attempt=attempt+1,updated_at=? WHERE id=?").run(workerId,x,n,n,r.id);const o=this.q.prepare('SELECT * FROM verification_jobs WHERE id=?').get(r.id);this.q.exec('COMMIT');return o}catch(e){this.q.exec('ROLLBACK');throw e}}heartbeatVerificationJob(id:string,w:string,s:number,status:VerificationJobStatus='RUNNING'){const n=nowIso(),x=new Date(Date.now()+s*1000).toISOString();return this.q.prepare("UPDATE verification_jobs SET status=?,claim_expires_at=?,last_heartbeat_at=?,updated_at=? WHERE id=? AND claimed_by=? AND status NOT IN ('COMPLETED','FAILED','CANCELLED')").run(status,x,n,n,id,w).changes===1}completeVerificationJob(id:string,w:string){const r=this.q.prepare("UPDATE verification_jobs SET status='COMPLETED',claim_expires_at=NULL,last_heartbeat_at=?,updated_at=? WHERE id=? AND claimed_by=?").run(nowIso(),nowIso(),id,w);if(r.changes!==1)throw new MadeProofError('JOB_LEASE_LOST','Verification job lease is no longer owned by this worker',409)}failVerificationJob(id:string,w:string,i:any){const r=this.q.prepare('SELECT attempt,max_attempts FROM verification_jobs WHERE id=? AND claimed_by=?').get(id,w)as any;if(!r)throw new MadeProofError('JOB_LEASE_LOST','Verification job lease is no longer owned by this worker',409);const retry=i.retryable&&Number(r.attempt)<Number(r.max_attempts),a=new Date(Date.now()+(i.backoffSeconds??Math.min(60,2**Number(r.attempt)))*1000).toISOString();this.q.prepare('UPDATE verification_jobs SET status=?,available_at=?,claimed_by=NULL,claim_expires_at=NULL,error_code=?,error_message=?,updated_at=? WHERE id=?').run(retry?'QUEUED':'FAILED',a,i.code,i.message.slice(0,2000),nowIso(),id)}cancelVerificationJob(w:string,r:string){const x=this.q.prepare('SELECT id FROM verification_jobs WHERE workspace_id=? AND run_id=?').get(w,r)as any;if(!x)return false;const y=this.q.prepare("UPDATE verification_jobs SET status='CANCELLED',claim_expires_at=NULL,updated_at=? WHERE id=? AND status NOT IN ('COMPLETED','FAILED','CANCELLED')").run(nowIso(),x.id);this.cancelRunnerJobsForVerification(x.id);return y.changes===1}
createOrGetRunnerJob(i:RunnerJobInput){const e=this.q.prepare('SELECT id FROM runner_jobs WHERE verification_job_id=? AND criterion_id=?').get(i.verificationJobId,i.criterionId)as any;if(e)return this.getRunnerJob(e.id);const id=newId('rjob'),n=nowIso();this.q.prepare("INSERT INTO runner_jobs(id,workspace_id,verification_job_id,run_id,check_id,criterion_id,type,required_capability,payload_json,status,attempt,max_attempts,available_at,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,'QUEUED',?,?,?,?,?)").run(id,i.workspaceId,i.verificationJobId,i.runId,i.checkId,i.criterionId,i.type,i.requiredCapability??null,JSON.stringify(i.payload),0,i.maxAttempts??3,n,n,n);return this.getRunnerJob(id)}getRunnerJob(id:string){const r=this.q.prepare('SELECT * FROM runner_jobs WHERE id=?').get(id)as any;return r?{...r,payload:parse(r.payload_json,{}),result:parse(r.result_json,null),error:parse(r.error_json,null)}:null}getRunnerJobForCriterion(v:string,c:string){const r=this.q.prepare('SELECT id FROM runner_jobs WHERE verification_job_id=? AND criterion_id=?').get(v,c)as any;return r?this.getRunnerJob(r.id):null}claimRunnerJob(i:any){this.recoverExpiredRunnerJobs();const n=nowIso(),x=new Date(Date.now()+i.leaseSeconds*1000).toISOString();this.q.exec('BEGIN IMMEDIATE');try{const rows=this.q.prepare("SELECT * FROM runner_jobs WHERE workspace_id=? AND status='QUEUED' AND available_at<=? AND attempt<max_attempts ORDER BY created_at LIMIT 25").all(i.workspaceId,n)as any[],caps=new Set(i.capabilities),r=rows.find(z=>!z.required_capability||caps.has(z.required_capability));if(!r){this.q.exec('COMMIT');return null}const y=this.q.prepare("UPDATE runner_jobs SET status='LEASED',runner_id=?,lease_token_hash=?,lease_expires_at=?,attempt=attempt+1,updated_at=? WHERE id=? AND status='QUEUED'").run(i.runnerId,i.leaseTokenHash,x,n,r.id),o=y.changes===1?this.getRunnerJob(r.id):null;this.q.exec('COMMIT');return o}catch(e){this.q.exec('ROLLBACK');throw e}}markRunnerJobRunning(i:any){const x=new Date(Date.now()+i.leaseSeconds*1000).toISOString(),r=this.q.prepare("UPDATE runner_jobs SET status='RUNNING',lease_expires_at=?,updated_at=? WHERE id=? AND workspace_id=? AND runner_id=? AND lease_token_hash=? AND status='LEASED' AND lease_expires_at>?").run(x,nowIso(),i.jobId,i.workspaceId,i.runnerId,i.leaseTokenHash,nowIso());if(r.changes!==1)throw new MadeProofError('RUNNER_JOB_TOKEN_INVALID','Runner job lease is invalid, expired or already consumed',409);return this.getRunnerJob(i.jobId)}completeRunnerJob(i:any){const r=this.q.prepare("UPDATE runner_jobs SET status='COMPLETED',result_json=?,error_json=NULL,lease_token_hash=NULL,lease_expires_at=NULL,updated_at=? WHERE id=? AND workspace_id=? AND runner_id=? AND lease_token_hash=? AND status IN ('LEASED','RUNNING') AND lease_expires_at>?").run(JSON.stringify(i.result),nowIso(),i.jobId,i.workspaceId,i.runnerId,i.leaseTokenHash,nowIso());if(r.changes!==1)throw new MadeProofError('RUNNER_JOB_TOKEN_INVALID','Runner job lease is invalid, expired or already consumed',409);return this.getRunnerJob(i.jobId)}failRunnerJob(i:any){const r=this.q.prepare("SELECT attempt,max_attempts FROM runner_jobs WHERE id=? AND workspace_id=? AND runner_id=? AND lease_token_hash=? AND status IN ('LEASED','RUNNING') AND lease_expires_at>?").get(i.jobId,i.workspaceId,i.runnerId,i.leaseTokenHash,nowIso())as any;if(!r)throw new MadeProofError('RUNNER_JOB_TOKEN_INVALID','Runner job lease is invalid, expired or already consumed',409);const retry=i.retryable&&Number(r.attempt)<Number(r.max_attempts),a=new Date(Date.now()+(i.backoffSeconds??Math.min(60,2**Number(r.attempt)))*1000).toISOString();this.q.prepare('UPDATE runner_jobs SET status=?,available_at=?,runner_id=NULL,lease_token_hash=NULL,lease_expires_at=NULL,error_json=?,updated_at=? WHERE id=?').run(retry?'QUEUED':'FAILED',a,JSON.stringify(i.error),nowIso(),i.jobId);return this.getRunnerJob(i.jobId)}recoverExpiredRunnerJobs(){const n=nowIso();return Number(this.q.prepare("UPDATE runner_jobs SET status=CASE WHEN attempt<max_attempts THEN 'QUEUED' ELSE 'TIMED_OUT' END,runner_id=NULL,lease_token_hash=NULL,lease_expires_at=NULL,available_at=?,updated_at=? WHERE status IN ('LEASED','RUNNING') AND lease_expires_at IS NOT NULL AND lease_expires_at<?").run(n,n,n).changes??0)}cancelRunnerJobsForVerification(id:string){return Number(this.q.prepare("UPDATE runner_jobs SET status='CANCELLED',lease_token_hash=NULL,lease_expires_at=NULL,updated_at=? WHERE verification_job_id=? AND status NOT IN ('COMPLETED','FAILED','CANCELLED','TIMED_OUT')").run(nowIso(),id).changes??0)}}
